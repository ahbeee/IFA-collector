[project]
name = "ifa-collector"
version = "0.1.0"
description = "Schema-driven In-band Flow Analyzer collector MVP"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
ifa-collector = "ifa_collector.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
